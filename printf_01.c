#include "main.h"


/**Handle two format specifier(s) e.i %d and %i
*N.B - There is no need to handle the flag character,
*No need to handle the field withd,
*No need to handle precision, and
*No need to handle  the length modifiers.
*/

int main(void)
{
int age;
age = 12;
printf("Age: %d", age);


int num;
num = 13;
printf("\nNew age: %i", num);

return (count);
}
